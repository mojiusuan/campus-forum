# 创建数据库步骤

## 📋 快速步骤

### 方法1：使用SQL脚本（推荐）

#### 步骤1：修改密码

编辑 `backend/scripts/create-database.sql` 文件，将 `your_password_here` 改为你想要的密码。

#### 步骤2：运行脚本

**如果psql命令可用：**
```bash
cd backend/scripts
psql -U postgres -f create-database.sql
```

**如果psql命令不可用（使用完整路径）：**
```bash
cd backend/scripts
& "D:\postgreSQL\bin\psql.exe" -U postgres -f create-database.sql
```

输入postgres用户密码后，脚本会自动创建数据库和用户。

---

### 方法2：手动执行SQL命令

#### 步骤1：连接PostgreSQL

```bash
# 如果psql命令可用
psql -U postgres

# 如果psql命令不可用
& "D:\postgreSQL\bin\psql.exe" -U postgres
```

输入postgres用户密码。

#### 步骤2：执行SQL命令

在PostgreSQL命令行中依次执行：

```sql
-- 创建数据库
CREATE DATABASE forum;

-- 创建用户（请修改密码）
CREATE USER forum_user WITH PASSWORD 'your_password_here';

-- 授权
GRANT ALL PRIVILEGES ON DATABASE forum TO forum_user;

-- 允许用户创建数据库（用于迁移）
ALTER USER forum_user CREATEDB;

-- 连接到forum数据库
\c forum

-- 授权schema权限
GRANT ALL ON SCHEMA public TO forum_user;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON TABLES TO forum_user;
ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT ALL ON SEQUENCES TO forum_user;

-- 退出
\q
```

---

## ✅ 验证创建结果

### 测试新用户连接

```bash
# 使用新创建的用户连接（会提示输入密码）
psql -U forum_user -d forum -h localhost

# 或使用完整路径
& "D:\postgreSQL\bin\psql.exe" -U forum_user -d forum -h localhost
```

如果连接成功，说明数据库和用户创建成功！

---

## 🔧 下一步：配置环境变量

创建数据库和用户后，需要更新 `backend/.env` 文件：

```env
DATABASE_URL="postgresql://forum_user:your_password_here@localhost:5432/forum?schema=public"
```

**重要：** 将 `your_password_here` 替换为你实际设置的密码！

---

## 🚀 然后运行迁移

```bash
cd backend
npm run prisma:generate
npx prisma migrate dev --name init
npm run prisma:seed
```

---

**文档版本：** v1.0  
**创建日期：** 2026-02-06
